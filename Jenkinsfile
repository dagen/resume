#!groovy

pipeline {
   agent none
   stages {
      stage 'Build'
         echo 'Building the Software'
      stage name: 'Test'
         echo 'Testing the software'

      stage name: 'Deploy'
         echo 'Deploying to production.'
   }
}
